---
title: "Plan — A0, les deux dettes"
type: plan
status: "EXÉCUTÉ le 2026-09-02 — T1..T9 ; hors ligne 154/154, live 22/22 + 14/14 + rejeu A2 60/60"
date: 2026-09-02
spec: docs/superpowers/specs/2026-09-02-a0-dettes-design.md
---

# A0 dettes — plan d'implémentation

> Lire la spec (faits 6-8, D1..D3). TDD : l'épreuve avant le code ; preuve finale par builds réels (T7-T8).

## T1 — `scripts/lib/gitea-pr-confirm.sh` + knob/timeout/pagination de `gitea-pr-comment.sh` (§9 a/b)
- [x] épreuves stub (open/closed/mismatch/404/500/non-objet/`\n` ; ONLY_IF_EXISTS ; pagination) rouges → lib → vertes ; `test-pr-comment.sh` vert.
## T2 — `provision-plan.sh` : forge relue avant le clone, checkout du SHA relu, faits (§9 c)
- [x] épreuves stub (refus avant clone, faits `refus`, zéro POST) ; parité : `test-a0-live` rejouée plus tard.
## T3 — `scripts/provision-plan-status.sh` (§9 d)
- [x] six corps, faits absents ⇒ relecture, non confirmé ⇒ aucun appel, BUILD_REF repli.
## T4 — `ci/Jenkinsfile.provision-plan` (facts + post de stage + post de pipeline gardé) ; `ci/Jenkinsfile.provision-apply` (garde avant node) ; `provisioning-request` documenté (§9 e/f/g)
- [x] câblage par numéros de ligne ; `test-provision-apply-wiring` réancré ; lint.
## T5 — `ci/Jenkinsfile.selfservice` (Formulaire + properties + withEnv) (§10 a)
- [x] câblage ; `test-provision-apply-wiring` §9 `L_PROPS < L_REQ` ; `test-palier-retention` ㉑ réancrées.
## T6 — `scripts/setup-selfservice-job.sh` (`XML_PARAMS`, `--print`, dérivation, amorçage, relecture) (§10 b)
- [x] épreuves `--print` (chaîne de test, mutation), miroir lib.
## T7 — Live plan : `test-a0-dettes-live.sh` (nouveau, à côté de `test-a0-live.sh`) — 22/22
- [x] porte (PR #390, plan #723, aucun statut en SUCCESS+ok), forgé branche A + numéro B (#727), branche inexistante (#728), hors provision/* (#729) ; piège : `result` est posé AVANT la fin du `post{}` — attendre `building=false` (les trois harnais corrigés).
## T8 — Live selfservice : `test-selfservice-form-live.sh` 14/14 (#31/#32) + rejeu `test-provision-apply-a2-live.sh` 60/60 (#87 → #33, #88, #89)
## T9 — Docs (GOAL, ENVIRONNEMENTS, spec statut), commit, gitea, mémoire
