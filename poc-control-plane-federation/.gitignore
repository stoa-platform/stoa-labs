# Generated by labctl — do NOT commit (inline comments are not allowed in
# .gitignore, so keep patterns on their own lines):
#   labctl-credentials.txt = synthetic secrets written 0600 by `labctl subscribe`
#   catalog-info.yaml      = Backstage entity generated by `labctl apply`
labctl-credentials.txt
catalog-info.yaml

# Generated by observability/data-prepper/render-pipelines.sh — do NOT commit:
#   pipelines.rendered.yaml = pipelines.yaml (gabarit, marqueur __FROM_ENV__)
#                             avec OPENSEARCH_PASSWORD substitué, 0600
observability/data-prepper/pipelines.rendered.yaml

# Local env
.env

# Sorties de build/release (É0) : les binaires ne se committent JAMAIS — ils se
# livrent versionnés via `make release` (dist/<version>/ + SHA256SUMS + SBOM).
# Les 4 noms nus = binaires `go build` posés à la racine du module (un
# governance-api de 10 Mo a failli partir en Git avec le périmètre ADR-077).
dist/
labctl/bin/
labctl/labctl
labctl/governance-api
labctl/onboarding-api
labctl/itsm-mock
