# secrets-baseline.txt — occurrences ACCEPTÉES par check-no-plaintext-secrets.sh
#
# Format : <fichier relatif à poc-control-plane-federation>|<VARIABLE>|<raison>
#
# POURQUOI CE FICHIER EXISTE. La garde échouait sur 23 occurrences préexistantes,
# donc elle échouait TOUJOURS — et une garde toujours rouge est une garde
# désactivée : plus personne ne regarde, et la 24e passe inaperçue. Ce fichier
# fige ce qui est connu et assumé, pour que TOUTE occurrence nouvelle rougisse.
#
# CE QU'IL NE FAIT PAS. La clé est (fichier, VARIABLE) — pas la valeur. Changer
# la VALEUR d'une entrée listée ne rouvre donc pas la garde. C'est un choix :
# hacher la valeur pour le détecter reviendrait à publier un condensat de secret
# dans un dépôt public, et pour des mots de passe courts c'est une invitation.
# Le contrôle des valeurs relève de la rotation, pas de ce grep.
#
# CE QUE CES VALEURS SONT. Des identifiants de LAB, dans un dépôt public depuis
# le 2026-07-30 : ils sont BRÛLÉS et doivent être traités comme tels. Les
# accepter ici n'est pas les absoudre — c'est cesser de faire crier la garde sur
# un passé qu'elle ne peut pas réparer, pour qu'elle serve à l'avenir.
#
# POUR RETIRER UNE ENTRÉE : supprimer le littéral du script (le lire depuis
# l'environnement), puis supprimer la ligne ici. La garde signale les entrées
# devenues inutiles.
scripts/get-oracle-token.sh|DEX_PASS|mot de passe de lab, brule (depot public depuis 2026-07-30)
scripts/repair-wm-dangling-policyaction.sh|WMPASS|mot de passe de lab, brule (depot public depuis 2026-07-30)
scripts/setup-ci-applier.sh|ADMIN_PASS|mot de passe de lab, brule (depot public depuis 2026-07-30)
scripts/setup-ci-horsprod.sh|ADMIN_PASS|mot de passe de lab, brule (depot public depuis 2026-07-30)
scripts/setup-onboarding-rbac.sh|ADMIN_PASS|mot de passe de lab, brule (depot public depuis 2026-07-30)
scripts/setup-onboarding-rbac.sh|DEV_PASS|mot de passe de lab, brule (depot public depuis 2026-07-30)
scripts/setup-provisioning-api.sh|KC_ADMIN_PASS|mot de passe de lab, brule (depot public depuis 2026-07-30)
scripts/setup-provisioning-api.sh|GWT_TOKEN|token de declenchement du lab (webhook), brule
scripts/setup-selfservice-job.sh|TRIGGER_TOKEN|token de declenchement du lab (webhook), brule
scripts/setup-user-deploy-job.sh|TRIGGER_TOKEN|token de declenchement du lab (webhook), brule
scripts/setup-user-vault-jwt.sh|KC_ADMIN_PASSWORD|mot de passe de lab, brule (depot public depuis 2026-07-30)
scripts/setup-vault-envs.sh|WM_DEV_PASS|mot de passe de lab, brule (depot public depuis 2026-07-30)
scripts/setup-vault-envs.sh|WM_REC_PASS|mot de passe de lab, brule (depot public depuis 2026-07-30)
scripts/setup-vault-envs.sh|WM_INT_PASS|mot de passe de lab, brule (depot public depuis 2026-07-30)
scripts/setup-vault.sh|WSO2_PASS|mot de passe de lab, brule (depot public depuis 2026-07-30)
scripts/setup-vault.sh|WM_PASS|mot de passe de lab, brule (depot public depuis 2026-07-30)
scripts/setup-vault.sh|WM_TRUSTSTORE_PASS|mot de passe de lab, brule (depot public depuis 2026-07-30)
scripts/setup-vault.sh|KC_ADMIN_PASS|mot de passe de lab, brule (depot public depuis 2026-07-30)
scripts/setup-wm-admin-self-proxy.sh|KC_ADMIN_PASSWORD|mot de passe de lab, brule (depot public depuis 2026-07-30)
scripts/spike-claim-runtime.sh|KC_ADMIN_PASS|mot de passe de lab, brule (depot public depuis 2026-07-30)
scripts/test-e0-blockers.sh|ASKPASS|faux positif : GIT_ASKPASS designe un helper, pas un secret
scripts/test-outbound-auth.sh|BE_PASS_A|mot de passe de lab, brule (depot public depuis 2026-07-30)
scripts/test-outbound-auth.sh|BE_PASS_B|mot de passe de lab, brule (depot public depuis 2026-07-30)
